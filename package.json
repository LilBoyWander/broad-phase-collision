{
  "name": "collision-pipeline-case-study",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Interactive broad-phase, narrow-phase, and collision-response comparison for dynamic circle simulations.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
